#include <stdio.h>
int n;
     char f(int a)
    {
        if(a==0)
        {
            printf("false");
        }
        else
        {
            printf("true");
        }
    }
int main()
{
scanf("%d", &n);
  f(n);
  return 0;
}

